{
  "name": "andicrochett-backend",
  "version": "0.1.0",
  "description": "API REST para AndiCrochett con SQLite centralizado",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "dev:https": "cross-env HTTPS_PORT=3443 node --watch src/server.js",
    "test": "node --test test/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "firebase-admin": "^12.7.0",
    "helmet": "^8.1.0",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "supertest": "^7.2.2"
  }
}
