import 'package:flutter/material.dart';

class AppColors {
  // Colores Primarios
  static const Color bronce = Color(0xFFC3C07D);
  static const Color lino = Color(0xFFE2DEC7);
  static const Color verdeOliva = Color(0xFF8F9B6E);

  // Colores Texto
  static const Color texto = Color(0xFF3F3F35);
  static const Color textoFuerte = Color(0xFF6F5B3E);
  static const Color resaltado = Color(0xFFC98B5A);

  // Colores de Estado
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFBF0000);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFFEFCFD0);

  // Colores hover y activo
  static const Color hover = Color(0xFFB7C1A3);

  // Colores de Fondo
  static const Color background = Color(0xFFF5F3E7);

  // Colores de Borde
  static const Color border = Color.fromARGB(112, 63, 63, 53);
  static const Color borderDark = Color(0xFFBDBDBD);
}
